/**
 * Firestore Collections
 *
 * Here we export our Firestore collection names so that we can reuse them
 * across both client and server libraries
 *
 */

export const USERS_COLLECTION = `users`;
export const ORGANIZATIONS_COLLECTION = `organizations`;
export const INVITES_COLLECTION = `invites`;
export const JOBS_COLLECTION = `jobs`;
export const PROPERTY_COLLECTION = `property`;
export const LOCATIONS_COLLECTION = `locations`;
export const SUPERVISOR_COLLECTION = `supervisors`;
export const LABOUR_CATALOGUE_COLLECTION = `labour_catalogues`;
export const INSTALLERS_COLLECTION = `installers`;
